Ensure you have a valid API key (replace 'your_api_key' in the code).
Create the required Chroma collections and ensure they are persisted in the directory (replace 'default_collection' and 'default_directory' in the code with actual values).